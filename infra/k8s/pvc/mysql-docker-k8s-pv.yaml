apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-docker-k8s-service-pv
spec:
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  hostPath:
    path: /mnt/data/mysql